package com.example.demo.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import com.hazelcast.config.Config;
import com.hazelcast.config.EvictionPolicy;
import com.hazelcast.config.MapConfig;

@Configuration
public class CacheConfig {
	/*
	 * @Bean public Config chacheConfig() { return new Config() // for creating
	 * cache .setInstanceName("hazelcast-cahe") .addMapConfig(new
	 * MapConfig().setName("coupon-cache") .setTimeToLiveSeconds(2000)
	 * .setMaxSizeConfig(new MaxSizeConfig(200,
	 * com.hazelcast.config.MaxSizePolicy.FREE_HEAP_SIZE))
	 * .setEvictionPolicy(EvictionPolicy.LRU)); }
	 */

}
